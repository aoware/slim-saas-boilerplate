`apis` folder is where wrapper to external apis can be found.

Credentials to these apis should NOT be stored in the code.
Those credentials would be stored in tables:
- configuration_definitions
- config_values